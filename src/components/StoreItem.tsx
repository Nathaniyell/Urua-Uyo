


type StoreItemProps = {
    id: number,
    title: string,
    price: number,
    imgUrl: string
}

export const StoreItem = ({id, title, price, imgUrl}:StoreItemProps) => {
  return (
    <div>StoreItem</div>
  )
}
